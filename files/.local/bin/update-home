#!/bin/sh
GREEN='\033[1;32m'
BLUE='\033[1;34m'
NC='\033[0m'

echo -e "\n${GREEN}Updating $HOME for ${BLUE}$(hostname)${GREEN}...${NC}\n"

uix home -L ~/.dotfiles reconfigure ~/.dotfiles/ns/systems/$(hostname).scm
