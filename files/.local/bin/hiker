#!/bin/sh
# Batch convert .HEIC files to .jpg

PHOTOS_DIR="$PWD"
[ $# -eq 1 ] && PHOTOS_DIR="$(readlink -f "$1")"

for f in "$PHOTOS_DIR"/*.HEIC;
do
	heif-convert $f ${f/%.HEIC/.png};
done

PNG_DIR="$PHOTOS_DIR/png"
mkdir -p "$PNG_DIR"
mv "$PHOTOS_DIR"/*.png "$PNG_DIR"
